apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment-blue
  labels:
    type: frontend
    version: blue
spec:
  replicas: 1
  selector:
    matchLabels:
      type: frontend
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        type: frontend
        version: blue
    spec:
      containers:
        - name: frontend
          image: dwg0245/front_facet:26
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          envFrom:
            - configMapRef:
                name: env-config