apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment-green
  labels:
    type: backend
    version: green
spec:
  replicas: 1
  selector:
    matchLabels:
      type: backend
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        type: backend
        version: green
    spec:
      containers:
        - name: backend
          image: alswn00/backend:20
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: env-config